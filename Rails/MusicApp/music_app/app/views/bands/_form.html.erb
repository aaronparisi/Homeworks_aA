<div class="app-form" id="new-band-form">
  <%= form_for([user, band]) do |f| %>
    <%= render_errors(band) %>

    <div class="fields">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <div class="actions">
      <%= f.submit "Add Band", class: "button", style: 'font-size: 18px; font-weight: 100' %>
    </div>
    
  <% end %>
</div>